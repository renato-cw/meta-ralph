# Meta-Ralph Environment Configuration
# Copy this file to .env and fill in your credentials
# NEVER commit .env to git!

# ============================================================================
# ZEROPATH CONFIGURATION
# Get credentials from: https://zeropath.com/settings/api-tokens
# ============================================================================
ZEROPATH_API_TOKEN_ID=
ZEROPATH_API_TOKEN_SECRET=
ZEROPATH_ORGANIZATION_ID=
ZEROPATH_REPOSITORY_ID=

# ============================================================================
# SENTRY CONFIGURATION
# Create token at: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:read, event:read, issue:read
# ============================================================================
SENTRY_AUTH_TOKEN=
SENTRY_ORGANIZATION=
SENTRY_PROJECT=

# ============================================================================
# GITHUB CONFIGURATION (optional, for GitHub Issues provider)
# Uses gh CLI authentication by default
# ============================================================================
# GITHUB_TOKEN=
# GITHUB_OWNER=
# GITHUB_REPO=

# ============================================================================
# CODECOV CONFIGURATION
# Create token at: https://app.codecov.io/account/settings (API Access)
# ============================================================================
CODECOV_API_TOKEN=
CODECOV_SERVICE=github              # github, gitlab, or bitbucket
CODECOV_OWNER=                      # Organization or username
CODECOV_REPO=                       # Repository name

# Optional: Coverage thresholds
# CODECOV_MIN_COVERAGE=50           # Only process files below this %
# CODECOV_TARGET_COVERAGE=80        # Target coverage in generated PRDs
# CODECOV_MAX_FILES=20              # Maximum files to process

# Optional: Path filters (comma-separated patterns)
# CODECOV_CRITICAL_PATHS=src/core,src/lib,src/services
# CODECOV_EXCLUDE_PATTERNS=*.test.*,*.spec.*,__tests__,__mocks__,node_modules

# ============================================================================
# RALPH SETTINGS (optional, have defaults)
# ============================================================================
# RALPH_MAX_ITERATIONS=10
# RALPH_BASE_BRANCH=main
# RALPH_LOG_DIR=.ralph-logs
