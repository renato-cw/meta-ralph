/**
 * Tag Management Components
 *
 * Components for managing user-defined tags on issues:
 * - TagBadge: Colored badge display for a single tag
 * - TagInput: Autocomplete input for adding/removing tags from issues
 * - TagManager: Full CRUD interface for managing tags
 */

export { TagBadge } from './TagBadge';
export { TagInput } from './TagInput';
export { TagManager } from './TagManager';
