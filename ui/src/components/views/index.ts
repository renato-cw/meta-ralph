/**
 * View components for displaying issues in different layouts.
 *
 * Provides components for:
 * - Grouped view with collapsible sections
 * - View toggle for switching between grouping options
 */

export { GroupedView } from './GroupedView';
export { GroupHeader } from './GroupHeader';
export { ViewToggle } from './ViewToggle';
